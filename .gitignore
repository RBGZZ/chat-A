# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# CodeGraph 本地索引(不提交,indexing 为本地决策)
.codegraph/

# Node / build output
node_modules/
dist/
*.tsbuildinfo
coverage/
.turbo/
_build/
__pycache__/
*.pyc

# Temp files
*.tmp
*.log

# Local secrets / env (not for upload)
.env
.env.local
.env.*.local

# Local SQLite memory store (runtime data, not for upload)
*.db
*.db-wal
*.db-shm

# Reference materials (not for upload)
reference/

# User's actual persona card (personal data; persona.example.yaml is the committed template)
persona.yaml
.claude/settings.local.json
.claude/worktrees/

# 测试音频产物(smoke:qwen / test:voice)
*.wav

# gstack 技能(工具安装产物,本机使用但不长期入库)
.agents/
