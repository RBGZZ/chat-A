{
  "name": "chat-a",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsx packages/client/src/cli.ts",
    "dev:brain": "tsx packages/client/src/cli-brain.ts",
    "start": "tsx packages/client/src/cli.ts",
    "test:voice": "tsx scripts/voice-text-to-wav.ts",
    "voice:harness": "tsx scripts/voice-harness.ts",
    "smoke:qwen": "tsx scripts/qwen-smoke.ts",
    "smoke:asr": "tsx scripts/asr-smoke.ts",
    "smoke:asr-rt": "tsx scripts/asr-realtime-smoke.ts",
    "desktop:dev": "pnpm --filter @chat-a/desktop dev",
    "desktop:rebuild": "pnpm --filter @chat-a/desktop rebuild",
    "desktop:build": "pnpm --filter @chat-a/desktop run build:bundle && pnpm --filter @chat-a/desktop package"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.22.4",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
